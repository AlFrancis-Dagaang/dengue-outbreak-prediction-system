# --- Core Python Libraries ---
numpy==1.26.4
pandas==2.2.2
matplotlib==3.9.0
seaborn==0.13.2

# --- Machine Learning & Deep Learning ---
scikit-learn==1.5.2           # For Random Forest and model evaluation
tensorflow==2.17.0            # For CNN model (Keras built-in)
torch==2.3.1                  # For YOLOv5 or YOLOv8 (PyTorch-based)
ultralytics==8.1.30           # YOLOv8 package (recommended for detection tasks)

# --- Image Processing & Computer Vision ---
opencv-python==4.10.0.84      # For capturing, preprocessing, and visualizing mosquito images
Pillow==10.3.0                # For image handling and saving

# --- Web Application / API ---
flask==3.0.3                  # For dashboard backend and endpoints
flask-cors==4.0.1             # To allow cross-origin access (e.g., JS frontend)
gunicorn==22.0.0              # For deployment (optional, used in servers)

# --- Data Visualization ---
plotly==5.22.0                # For interactive charts in the dashboard
dash==2.17.1                  # Optional: If using Plotly Dash for dashboard

# --- Misc / Utilities ---
requests==2.32.3              # For HTTP requests (if fetching weather data)
joblib==1.4.2                 # For saving/loading trained RF models
tqdm==4.66.5                  # For progress bars during training
